{
  "name": "wresizer",
  "version": "0.1.0",
  "title": "Resize, watermark and optimize images like a pro",
  "author": "Serj Lavrin",
  "private": false,
  "dependencies": {
    "async": "^2.1.4",
    "grunt": "^1.0.1",
    "grunt-contrib-clean": "^1.0.0",
    "grunt-contrib-watch": "^1.0.0",
    "grunt-gm": "^0.4.3",
    "grunt-newer": "^1.1.1",
    "grunt-size-report": "^0.1.1",
    "grunt-tinypng": "^0.6.1",
    "jit-grunt": "^0.10.0",
    "time-grunt": "^1.2.1"
  },
  "engines": {
    "node": ">=0.12.0",
    "iojs": ">=1.6.1"
  },
  "scripts": {
    "test": "grunt build && grunt test && git status | grep 'working directory clean' >/dev/null || (echo 'Build task should not generate or change files outside build folder'; exit 1)"
  }
}
